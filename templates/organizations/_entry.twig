<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

{{ entry.title }}
{% if entry.logo.one()|length %}
    <img src="{{ entry.logo.one().getUrl('wide250') }}" alt="{{ entry.title }}">
{% endif %}
</body>
</html>

